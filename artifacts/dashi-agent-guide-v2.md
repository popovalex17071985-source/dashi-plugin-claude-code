# Свой AI-агент как Jarvis — с нуля за 15 минут (v6)

**Сервер + Claude Code на подписке + плагин dashi + Telegram. Работает 24/7,
понимает голос, помнит контекст, сам переподнимается, ноль кнопок
подтверждения.**

Инструкция для человека без опыта. Делай ровно по шагам сверху вниз — каждый
шаг говорит, что вставить и что ты должен увидеть. Ничего не пропускай: шаги
идут в том порядке, в котором нужны.

> v4 (19.08.2026): пошаговый быстрый путь, справка по командам, сторож
> контекста. Прежний ручной путь (14 шагов) — ниже, он нужен, когда что-то
> пошло не так и хочется понимать, что происходит.
>
> v5 (20.08.2026), после живого ремонта и перевода чужого агента на установщик:
> раздел про месячный перелогин (главная причина «агент вдруг замолчал»),
> правила прав («агент всё делает сам, никаких „сходи под root"»), точечный sudo
> на свой сервис, переписанная схема systemd (Type=simple + держатель + умное
> нажатие стартовых диалогов), раздел «перевод ручного агента на установщик»,
> новые строки в траблшутинге.
>
> v6 (21.08.2026), после аудита автономии: месячный перелогин теперь делается
> командой `/relogin` прямо из чата (терминал — запасной путь); `/restart` из
> чата; вместо набора sudoers-правил — один root-owned скрипт `dashi-ctl`;
> сторож моста в dashi-run (tmux жив, а бот молчит — сервис перезапустится
> сам); повторный прогон установщика обновляет плагин; агент сам правит свой
> канальный конфиг и чинит владельца файлов.

---

# Пошаговая установка

## Шаг 0. Собери три вещи (10 минут)

Без них дальше идти нельзя.

1. **VPS** — любой хостинг, Ubuntu 22.04 или новее, 2 ГБ памяти. На почту
   придут: **IP-адрес**, логин `root` и **пароль**.
2. **Телеграм-бот.** В Telegram найди **@BotFather** → напиши `/newbot` → он
   спросит имя (любое) и username (обязан заканчиваться на `bot`) → пришлёт
   **токен** вида `7712345678:AAH...`. Скопируй его.
3. **Свой id.** Найди **@userinfobot** → нажми Start → он пришлёт число вроде
   `140141496`. Это твой id.
4. **Найди своего нового бота по username и нажми Start.** Без этого он не
   имеет права написать тебе первым.
5. **Подписка Claude** — Pro или Max, оформлена на claude.ai. Для круглосуточной
   работы бери Max.
6. *(по желанию)* **Ключ Groq** для голосовых: console.groq.com → Create API Key →
   строка на `gsk_`. Бесплатно, можно добавить потом.

> ⚠️ Токен бота — это пароль от бота. Засветил в чате или на скриншоте — сразу
> @BotFather → `/revoke`, получишь новый.

---

## Шаг 1. Зайди на сервер

На Mac/Linux открой Терминал, на Windows — приложение «Терминал» или PowerShell,
и вставь (подставь свой IP):

```
ssh root@123.45.67.89
```

Спросит «are you sure» — напиши `yes`. Спросит пароль — вставь его. **Пароль при
вводе не отображается, это нормально**, просто вставь и нажми Enter.

**Что ты должен увидеть:** приглашение вида `root@vps:~#`. Значит, ты внутри
сервера, и всё дальнейшее выполняется там.

---

## Шаг 2. Запусти установщик

Вставь обе строки целиком:

```
curl -fsSL https://raw.githubusercontent.com/popovalex17071985-source/dashi-plugin-claude-code/main/scripts/install-agent.sh -o install-agent.sh
sudo bash install-agent.sh
```

Он спросит по очереди:

- **имя агента** — латиницей, без пробелов, например `jarvis`;
- **токен бота** — из шага 0;
- **твой id** — число из шага 0;
- **ключ Groq** — вставь или просто нажми Enter, если пропускаешь.

Дальше он сам поставит систему, Node.js, Claude Code, заведёт отдельного
пользователя, Bun, рабочую папку, плагин, конфиг, хуки и автозапуск. Это
5–10 минут, в это время ничего делать не надо.

**Что ты должен увидеть:** зелёные галочки `✓` и в какой-то момент — просьбу
войти в Claude. Это шаг 3.

---

## Шаг 3. Войди в Claude (единственное ручное место)

Установщик остановится и попросит войти. Вставь:

```
su - agent
claude
```

Дальше:

1. Стрелками выбери **«Claude account»** (НЕ «API key») и нажми Enter.
2. Claude покажет длинную ссылку. Терминал разорвёт её на несколько строк —
   скопируй все куски и склей в ОДНУ строку без переносов и пробелов, иначе
   браузер её не откроет. Открывать — **на своём компьютере**, не на сервере.
3. Войди аккаунтом, на котором оформлена подписка.
4. Браузер покажет код — скопируй его и вставь обратно в терминал, Enter.

**Что ты должен увидеть:** Claude запустится и покажет своё приглашение. Теперь
нажми `Ctrl+C`, затем напиши `exit` — вернёшься под root.

> ⚠️ Из России сам сайт claude.ai может не открываться даже с VPN, а вход по
> этой ссылке — работает: это отдельная страница входа. Перенести вход с Mac
> (`scp ~/.claude`) или из десктопного приложения НЕ выйдет — там другие токены.

---

## Шаг 4. Запусти установщик второй раз

Той же командой:

```
sudo bash install-agent.sh
```

Имя агента он спросит снова — введи **то же самое**, буква в букву, иначе
начнёт заводить второго агента с нуля. Токен и id не спросит, они уже
записаны. Всё сделанное пропустит и доделает остальное — включит службу.

**Что ты должен увидеть:** в конце — сообщение, что сервис запущен.

> Скрипт можно гонять сколько угодно раз: он доделывает недостающее и не трогает
> личное (CLAUDE.md, память, конфиг с токеном). Прервался, потерял связь,
> ошибся — просто запусти снова. Плюс каждый повторный прогон **обновляет сам
> плагин** до свежего main — фиксы моста доезжают без ручного git pull — и
> возвращает юзеру владение всей его домашней папкой (root-починки больше не
> оставляют мин).

---

## Шаг 5. Проверь, что живой

Напиши боту в Telegram: **привет**.

**Что ты должен увидеть:** ответ в течение нескольких секунд.

Теперь проверь автономию — попроси:

> создай файл /tmp/proba.txt с текстом привет и прочитай его

**Что ты должен увидеть:** он сделал и отчитался, ни разу не спросив
подтверждения. Значит, кнопки отключены правильно.

Не ответил? Смотри «Если что-то пошло не так» ниже.

---

## Шаг 6. Дай ему характер (5 минут, но лучше сразу)

Установщик кладёт болванку. Одним сообщением боту:

> Проведи со мной интервью и перепиши свой CLAUDE.md: кто я, чем занимаюсь, как
> ко мне обращаться, как ты должен отвечать, что мне важно.

**Что ты должен увидеть:** он задаёт вопросы, потом сообщает, что переписал файл.
После этого он отвечает уже как твой агент, а не как безликий помощник.

---

## Шаг 7. Проверь память

Скажи ему: **запомни, что я работаю в часовом поясе Екатеринбурга**. Потом
напиши `/new` и спроси: **в каком я часовом поясе?**

**Что ты должен увидеть:** он помнит — значит, память между разговорами работает.
Не помнит — попроси: «настрой себе память по инструкции из своего CLAUDE.md».

---

# Команды агента в Telegram

Пиши их боту как обычные сообщения. Все команды видны и по `/help`.

| Команда | Что делает | Когда нужна |
|---|---|---|
| `/help` | список команд | забыл, что есть |
| `/status` | как агент себя чувствует: жив ли, сколько занято контекста, какая модель | «он завис или думает?» |
| `/stop` | прервать текущую работу | ушёл не туда, надо остановить |
| `/compact` | сжать разговор: суть остаётся, лишнее уходит | разговор длинный, а тему надо продолжить |
| `/new` | начать разговор с чистого листа | тема закрыта, переходим к другой |
| `/mirror on\|off` | показать в Telegram живое зеркало терминала | хочется видеть, что он там делает |
| `/keys` | панель кнопок: тап = нажатие в сессии | Claude Code спросил что-то своим диалогом |
| `/cc` | команды самого Claude Code, например `/cc model opus` | сменить модель и прочие настройки |
| `/relogin` | обновить вход в Claude: бот пришлёт ссылку, код кидаешь обычным сообщением | раз в месяц, когда вход протух (раздел ниже) |
| `/restart` | перезапустить мост целиком через systemd | бот тупит или подвисает, но команды ещё слышит |
| `/update` | обновить мост до свежей версии: бэкап → обновление → перезапуск, при сбое откат | советник написал «вышло обновление», или ты сам знаешь, что пора; `/update force` — если правил код плагина руками |

`/relogin`, `/restart` и `/update` обрабатывает сам мост, а не Claude — поэтому они
работают даже с протухшим логином, когда агент на всё остальное молчит.

### Семантическая память (опция)

По умолчанию агент помнит файлами (`memory/MEMORY.md` + заметки) — это поиск по
словам. Ключ `--openai-key KEY` при установке (или повторном прогоне) включает
OpenViking: docker-контейнер на той же машине (~400 МБ RAM, порт 1933 только на
localhost), эмбеддинги через твой ключ OpenAI (копейки), плагин памяти в Claude Code —
агент сам вспоминает релевантное на каждое сообщение и сам запоминает важное.
Ключ лежит только в `~agent/.openviking/ov.conf` (600). Сменить ключ — повторный
прогон с новым `--openai-key`. Выключить — `docker rm -f openviking` и
`claude plugin uninstall claude-code-memory-plugin@openviking-plugin` под агентом.

Важное про `/stop`: он посылает в сессию Escape. Обычно останавливает, но
посреди тяжёлой операции может не успеть — это нормально, повтори.

---

# Контекст: что это и почему агент вдруг тупеет

**Простыми словами.** У агента есть «оперативная память разговора» — всё, что вы
уже написали друг другу за эту сессию. Она конечна. Чем длиннее разговор, тем
меньше свободного места, и тем хуже он держит детали: начинает переспрашивать,
терять договорённости, отвечать мимо.

Со стороны это выглядит как «поглупел». На деле — просто кончается место.

**Что делать.** Два лечения, разница простая:

- `/compact` — **сжать**. Он ужимает переписку до сути и продолжает ту же тему.
  Бери, когда работа не закончена.
- `/new` — **начать заново**. Полностью чистый лист, тема забывается (то, что он
  сохранил в память файлами, остаётся). Бери, когда переходишь к другому делу.

**Он предупредит сам.** В установщик вшит сторож: когда занято около 70%, агент
одной строкой скажет, что место заканчивается и пора `/compact`; около 85% —
скажет прямо в начале ответа, что дальше начнёт терять детали. Повторять каждый
ход он не будет — предупреждение приходит один раз на каждый порог.

Хочешь посмотреть цифру сам — `/status`, там строка с процентом заполнения.

**Правило на каждый день:** новая тема — `/new`. Это самая дешёвая привычка:
и отвечает точнее, и подписка тратится медленнее.

---

# Что дальше (по желанию, каждое — 5 минут)

**Голос.** Не вписал ключ Groq при установке — добавь строку
`GROQ_API_KEY=gsk_...` в `/etc/dashi-plugin/<имя>/channel.env` и перезапусти:
`systemctl restart dashi-<имя>`.

**Семантическая память (OpenViking).** Файловая память работает сразу и надолго
хватает. Когда фактов накопится столько, что поиск по индексу начнёт промахиваться
(агент сам напишет об этом), сверху ставится OpenViking — он ищет по смыслу, а не
по словам. Нужен Docker:

```
claude plugin marketplace add Castor6/openviking-plugins
claude plugin install claude-code-memory-plugin@openviking-plugin
```

Дальше ему нужен свой конфиг и поднятый docker-compose — поэтому в установщик это
не зашито. Подключается сбоку, ничего из накопленного не ломает.

**Бот-ремонтник** — страховка на случай, когда основной агент завис: второй
тупой бот с командами `/status`, `/restart`, `/logs`. Смысл его тупости в том,
что он не виснет вместе с основным. Инструкция — «Ручной путь», Часть G.

**Советник.** Раз в неделю сам пишет тебе, если что-то пора поменять: память
переросла файлы, диск кончается, сервис часто падает. Ставится установщиком, от
тебя ничего не нужно. Крон гоняется под юзером агента, не под root: скрипт
лежит в папке агента, и root-крон оттуда был бы дырой (перезаписал файл —
получил произвольный код под root).

---

# Раз в ~30 дней: вход протухает (агент вдруг замолчал)

Вход в Claude по подписке живёт около 30 дней, потом агент молча перестаёт
отвечать. Это не поломка, это ритуал. И с v6 он делается прямо из чата.

**Он сам скажет, когда пора.** Сторож на сервере видит экран «Login expired»
и пишет тебе в Telegram: «Вход в Claude протух — отправь /relogin»
(не чаще раза в 6 часов, спамить не будет).

**Основной путь — `/relogin` из чата, терминал не нужен:**

1. Отправь боту `/relogin`. Мост сам наберёт `/login` в сессии и пришлёт
   тебе кликабельную ссылку.
2. Открой ссылку и войди **В ТОТ ЖЕ аккаунт Anthropic, что и раньше**
   (сменишь аккаунт — уедут модель и лимиты). Появится код — скопируй.
3. Пришли код **обычным сообщением в тот же чат**. Мост сам вставит его в
   сессию и подтвердит, что вход обновлён.

На код даётся 15 минут; не успел — просто отправь `/relogin` ещё раз.
Работает это потому, что `/relogin` обрабатывает сам мост, а не Claude —
протухший логин ему не мешает.

**Запасной путь — через терминал.** Нужен, только если лежит и сам мост:
бот не реагирует даже на `/relogin`. Тогда по-старому, под root:

1. `su - agent`, затем `tmux capture-pane -pt channel-<имя> | tail -30` —
   убедись, что на экране «Login expired · Please run /login».
2. `tmux attach -t channel-<имя>` — ты внутри сессии агента.
   Набери `/login`, Enter — появится длинная ссылка.
3. Ссылка из терминала обычно НЕ копируется целиком. Рабочий способ — QR-код:
   выйди из tmux (Ctrl+B, отпустить, потом D) и выполни:

   ```
   tmux capture-pane -pJt channel-<имя> -S -100 | grep -o "https://[^ ]*" | tail -1 | qrencode -t ANSIUTF8
   ```

   (если `qrencode` не найден — разово под root: `apt install -y qrencode`).
   Отсканируй QR телефоном — ссылка откроется в браузере телефона.
4. Войди в тот же аккаунт, скопируй код.
5. `tmux attach -t channel-<имя>` → вставь код → Enter → «Login successful» →
   Enter → выйди (Ctrl+B, D).
6. Напиши боту — должен ответить.

Пока код не вставлен, экран `/login` не закрывай — ссылка одноразовая.

---

# Чтобы агент всё делал сам, а не гонял тебя под root

Четыре правила. Каждое — из реальной поломки, когда агент «вдруг разучился»
делать то, что делал вчера.

**1. Никогда не поднимай агента руками под root — и не заходи `su agent` без
дефиса.** Правильный перезапуск всегда один:

```
systemctl restart dashi-<имя>
```

Ручной tmux — только если сервиса нет совсем. И тогда строго после
`su - agent` **с дефисом**: без дефиса шелл остаётся с root-овским окружением,
агент не находит bun, и мост в Telegram мёртв при живом Claude (на экране
«bun: not found»). Проверка, что ты зашёл правильно: приглашение показывает
`~`, а не `/root`.

**2. Всё, с чем агент работает — отдай ему во владение.** Запустили что-то под
root в его папках — файлы перекрасились в root, и агент честно говорит «не могу
записать». Свежий установщик страхует дважды: каждый его прогон возвращает
юзеру владение ВСЕЙ домашней папкой, а у самого агента есть команда
самолечения — `sudo dashi-ctl-<имя> fix-owner`. Так что «файл принадлежит
root» агент теперь чинит сам, без тебя. Правило на будущее осталось: новый
проект для агента вне его домашней папки → сразу `chown -R agent:agent`.

**3. Самообслуживание — через ОДИН root-owned скрипт, а не набор sudoers-строк.**
Свежий установщик (от 21.08.2026) кладёт `/usr/local/bin/dashi-ctl-<имя>` —
принадлежит root, агенту разрешён через sudo одной строкой в
`/etc/sudoers.d/dashi-<имя>`. Умеет ровно шесть вещей:

```
sudo dashi-ctl-<имя> restart        # перезапустить свой сервис
sudo dashi-ctl-<имя> status         # состояние сервиса
sudo dashi-ctl-<имя> logs 200       # последние строки журнала
sudo dashi-ctl-<имя> fix-owner      # вернуть себе владение своими файлами
sudo dashi-ctl-<имя> vacuum         # ужать журнал systemd, если кончился диск
sudo dashi-ctl-<имя> update-claude  # обновить Claude Code
```

Почему так, а не отдельные права на systemctl/journalctl/chown: wildcard-права
в sudoers эскалируются до root — `journalctl` с любыми аргументами читает
произвольные файлы через `--file=`, а `chown` по глобу уводится через
симлинки. В root-owned скрипте пути зашиты намертво, аргументы проверяются
внутри, а переписать его агент не может. Полный sudo агенту не давать никогда.
Читать журнал своего сервиса агент может и вовсе без sudo — установщик
добавляет юзера в группу `systemd-journal`.

**4. Агент должен ЗНАТЬ свои права.** Установщик вписывает в шаблон CLAUDE.md
секцию «Самообслуживание»: какой у него сервис, что умеет dashi-ctl, что
channel.env он правит сам. Без неё права лежат мёртвым грузом, а агент
по-прежнему гоняет тебя в терминал. У уже установленных агентов CLAUDE.md
не перезаписывается — попроси агента самого дописать себе такую секцию
(образец — в шаблоне внутри `scripts/install-agent.sh`, секция
«Самообслуживание»).

Заодно про конфиг канала: `channel.env` теперь `660 root:юзер` — новый ключ
(Groq, chat_id и т.п.) агент вписывает сам и сам перезапускается, в терминал
ходить не надо.

---

# Если что-то пошло не так

## Ставил до 19.08.2026 — проверь пять мест

Тестовый прогон инструкции 19.08 вскрыл пять дыр, все закрыты в установщике.
Если твой агент ставился раньше и ведёт себя странно — перекачай установщик и
прогони заново, либо проверь руками:

| Симптом | Причина | Лечение |
|---|---|---|
| `bun: command not found` на шаге Hooks | bun не виден из-под служебного пользователя | перезапустить свежий установщик |
| сервис вечно `activating`, в логе `no server running` | Claude при старте спрашивает про режим без подтверждений, а служба отвечала «нет» | свежий установщик (жмёт «вниз + Enter») |
| бот отвечает, но нет карточки с шагами | выключен приёмник событий, случайный порт или нет токена | в `channel.env`: `TELEGRAM_WEBHOOK_HOST=127.0.0.1`, `TELEGRAM_WEBHOOK_PORT=8089`, `TELEGRAM_WEBHOOK_TOKEN=<любая случайная строка>`; в `state/telegram/config.json`: `{ "webhook": { "enabled": true }, "progress": { "enabled": true } }` |
| хуки не прописаны (`grep -c dashi-channel-hook ~/.claude/settings.json` = 0) | шаг молча пропускался | свежий установщик |
| машина зависает на 1 ГБ памяти | нет свопа | свежий установщик делает своп сам |

Ставил до **20.08.2026** — ещё три, все лечатся одним прогоном свежего установщика:

| Симптом | Причина |
|---|---|
| сервис «Started» и через секунду inactive (dead) | Type=forking терял главный процесс tmux — теперь Type=simple + держатель |
| в логе `duplicate session` и рестарты по кругу | осиротевшая tmux-сессия — теперь чистится перед стартом |
| Claude сам «выходит» на старте | слепые нажатия попадали в «Exit» — теперь кнопки жмутся по содержимому экрана |

Ставил до **21.08.2026** — ещё пачка, лечится тем же прогоном (он же теперь
сам обновляет плагин с GitHub, так что `/relogin` и `/restart` тоже приедут):

| Симптом | Причина |
|---|---|
| tmux жив, systemd доволен, а бот молчит | мост умирал внутри живой сессии — теперь dashi-run следит за вебхук-портом (127.0.0.1:8089): порт был жив и пропал на 2 проверки подряд → сервис перезапускается целиком |
| на слабом VPS Claude «не успевает» пройти стартовые диалоги | прожиматель ждал 45 сек — теперь до 2 минут |
| журнал systemd съел диск | не было потолка — теперь drop-in ограничивает 500 МБ |
| агент после чужой root-починки «не могу записать» и зовёт тебя | не было fix-owner — теперь dashi-ctl + chown всей домашней папки на каждом прогоне |

> Скачал установщик, а фиксы «не подействовали»? GitHub отдаёт raw-файлы с
> кэшем до ~5 минут. Качай с довеском `?v=2` (цифру меняй) в конце ссылки.

## Агент ставился руками по старой инструкции — перевод на установщик

Проверено вживую 20.08.2026. Ничего личного он не перезапишет: CLAUDE.md,
память и конфиг с токеном трогаются только если их нет.

1. Бэкап (под юзером агента): `tar czf ~/backup-$(date +%F).tar.gz ~/.claude-lab/<имя> ~/.claude ~/.claude.json`
2. Установщик под root с ТВОИМИ путями: `bash install-agent.sh --user <юзер-агента>`,
   на вопрос об имени — имя workspace-папки (то, что в `~/.claude-lab/<имя>`).
   Токен и id — те же, что были.
3. Убить ручную tmux-сессию старого запуска: `su - <юзер> -c "tmux kill-server"` —
   иначе старый и новый Claude дерутся за одного бота, и он глохнет.
4. `systemctl restart dashi-<имя>` → status → active (running) → написать боту.
   Дальше руками ничего не поднимать: перезапуск — `systemctl restart` (или
   `/restart` боту), месячный перелогин — `/relogin` из чата.


Полезные команды (выполняй на сервере под root):

```
systemctl status dashi-<имя>          # жив ли
journalctl -u dashi-<имя> -n 50       # что в логах
systemctl restart dashi-<имя>         # перезапустить
su - agent -c 'tmux attach -t channel-<имя>'   # заглянуть внутрь сессии (выход: Ctrl+B, потом D)
```

Встроенная диагностика — ничего не ломает, только смотрит:

```
su - agent
cd ~/.claude-lab/<имя>/.claude/dashi-plugin-claude-code/plugin
bun ../skills/doctor-dashi-plugin/scripts/doctor.ts --plugin-dir "$PWD"
```

Частые симптомы разобраны в конце файла — раздел «Быстрый траблшутинг».

**Правило номер один:** вылезла ошибка — не гадай. Скопируй её целиком и спроси
у Claude (в claude.ai или у своего агента) «что делать по шагам».

---
---

# Ручной путь

Дальше — прежний пошаговый гайд: то же самое, но руками. Нужен, если установщик
не подошёл (нестандартная система, свои пути), если хочется понимать каждый шаг,
или для разбора поломки.

## Условные обозначения

- **[ТЕРМИНАЛ]** — команда на твоём Mac/ПК (до захода на сервер).
- **[СЕРВЕР]** — команда внутри сервера (после SSH).
- **[БОТУ]** — задача самому агенту (пишешь ему в Telegram).
- **[БРАУЗЕР]** — действие на сайте.

---

## Три решения, которые экономят день (прочти до старта)

Мы набили эти шишки — ты не набивай:

1. **Агент живёт под ОТДЕЛЬНЫМ не-root пользователем, а НЕ под root.**
   Полная автономия («агент сам всё делает, ничего не подтверждаешь») под root
   технически НЕВОЗМОЖНА — Claude Code отказывается работать в режиме без
   подтверждений от имени root. Поэтому мы сразу создаём юзера `agent` и всё
   ставим под ним. Если поставить под root, а потом захотеть автономию —
   придётся переносить всё заново (мы так и потеряли время).

2. **Длинный код НЕ копируется через Telegram надёжно.** Копипаст кода из чата в
   редактор на сервере глючит (особенно на Mac). Поэтому файлы со скриптами
   пусть пишет САМ агент прямо на диск сервера — не через чат.

3. **Вход в Claude делаем прямо на сервере** (способ с браузером). Копирование
   логина с Mac и вход через приложение — НЕ работают для CLI.

---

# ЧАСТЬ A. Сервер, пользователь, Claude Code, вход

## Шаг 1 — Арендовать VPS

Зачем: сервер работает всегда — агент онлайн 24/7 без твоего компьютера.

- Провайдер любой (VDSina, Timeweb, Selectel, Aeza, Hetzner).
- Тариф: **Ubuntu 22.04 или 24.04**, минимум **2 ГБ RAM** (лучше 4), 1–2 CPU.
- Провайдер пришлёт **IP, логин (root), пароль** — запиши.

## Шаг 2 — Зайти на сервер

Открой Терминал (Mac: Cmd+Пробел → «Терминал». Windows: Terminal/PowerShell).

**[ТЕРМИНАЛ]** (подставь свой IP):
```
ssh root@ТВОЙ_IP
```
На «yes/no» → `yes`, затем пароль (символы при вводе не видно — это норма).
Появилась строка `root@...#` — ты внутри. Дальше всё **[СЕРВЕР]**.

## Шаг 3 — Обновить систему и поставить Node.js + Claude Code

**[СЕРВЕР]** (по одной строке):
```
apt update && apt -y upgrade
curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
apt install -y nodejs git unzip
npm install -g @anthropic-ai/claude-code
```
Проверка: `claude --version` — показал версию, идём дальше.

> ⚠️ ГРАБЛЯ: `unzip` часто не стоит по умолчанию, а он нужен установщику Bun
> дальше. Поэтому ставим сразу (`unzip` в строке выше).

## Шаг 4 — Создать отдельного пользователя `agent` (КЛЮЧЕВОЙ шаг)

Зачем: см. «решение №1» выше — под root не будет полной автономии.

**[СЕРВЕР]**:
```
adduser --disabled-password --gecos "" agent
```
Теперь входим под него:
```
su - agent
```
Строка сменилась на `agent@...$` — теперь ты работаешь как `agent`.

> ⚠️ ГРАБЛЯ: ВСЁ ниже (Bun, плагин, вход в Claude, конфиг) делаем из-под
> `agent`, не из-под root. Если случайно окажешься под root (`root@...#`) —
> вернись: `su - agent`.

## Шаг 5 — Вход в Claude по подписке (без API-ключа)

Зачем: агент работает на твоей подписке (Pro/Max; для 24/7 бери Max).

**[СЕРВЕР]** (под `agent`):
```
claude
```
Выбери вход **«Claude account (подписка)»**, НЕ «API key». Claude покажет
ссылку — открой её в браузере на своём компьютере, залогинься аккаунтом с
подпиской, скопируй показанный код и вставь обратно в терминал сервера.

Проверка: `claude -p "скажи привет одной строкой"` — пришёл ответ, вход есть.
Выйди из этого пробного запуска (Ctrl+C).

> ⚠️ ГРАБЛЯ (Россия): вход через сайт claude.ai может не открываться без VPN, а с
> некоторыми VPN — тоже. Не мучай claude.ai. Способ выше (`claude` на сервере →
> ссылка → код) — рабочий: авторизуешься через OAuth-страницу, а не через личный
> кабинет. Копировать вход с Mac (`scp ~/.claude ...`) или войти в десктоп-
> приложение и «перенести» токен — НЕ работает: у приложения и у CLI токены
> разные.

---

# ЧАСТЬ B. Плагин dashi (мост Telegram ↔ агент)

Зачем именно плагин, а не самописный бот: он держит ОДНУ живую сессию Claude
Code и пушит в неё сообщения из Telegram. Что даёт: расход внутри подписки (а не
поштучная оплата API), память между сообщениями, из коробки — приём голосовых,
пузырёк прогресса, авто-рестарт связи, безопасная отправка (прячет секреты,
режет длинное).

## Шаг 6 — Bun (движок плагина)

**[СЕРВЕР]** (под `agent`):
```
curl -fsSL https://bun.sh/install | bash
```
Перелогинься, чтобы `bun` подхватился в PATH:
```
exit          # вышли из agent обратно в root
su - agent    # снова зашли — теперь bun в PATH
bun --version
```
Показал версию — ок.

## Шаг 7 — Папки агента (workspace + секреты)

Зачем: одна «личность» = один workspace со своим CLAUDE.md; секреты держим
отдельно и закрытыми.

**[СЕРВЕР]**:
```
mkdir -p ~/.claude-lab/myagent/.claude ~/.claude-lab/myagent/secrets
cd ~/.claude-lab/myagent/.claude
```

## Шаг 8 — Склонировать плагин ВНУТРЬ workspace

> ⚠️ ГРАБЛЯ: расположение критично. Claude Code грузит характер (CLAUDE.md) по
> текущей папке, а сам плагин ищется относительным путём. Кладём внутрь `.claude`
> и запускаем всё потом ИЗ ПАПКИ `plugin` (см. Шаг 13). Иначе — ошибка «no MCP
> server configured with that name».

**[СЕРВЕР]** (ты в `~/.claude-lab/myagent/.claude`):
```
git clone https://github.com/qwwiwi/dashi-plugin-claude-code.git
cd dashi-plugin-claude-code/plugin
bun install
```

## Шаг 9 — Telegram-бот и твой ID

**[БРАУЗЕР/Telegram]**:
1. @BotFather → Start → `/newbot` → имя и username (должен кончаться на `bot`) →
   пришлёт **ТОКЕН** (пароль к боту, никому не показывай).
2. @userinfobot → Start → пришлёт твой **ID** (число).
3. Найди своего бота, нажми **Start**, напиши «привет» (без этого бот не сможет
   писать тебе первым).

> ⚠️ ГРАБЛЯ: если токен где-то засветился (скинул в чат, на скрине) — сразу
> @BotFather → `/revoke` → получи новый. Утёкший токен = чужой доступ к боту.

## Шаг 10 — Конфиг (channel.env)

> ⚠️ ГРАБЛЯ: не набирай имена переменных вручную из головы — легко ошибиться.
> Копируй готовый пример и меняй только ЗНАЧЕНИЯ.

**[СЕРВЕР]** (из папки `plugin`):
```
cp ../examples/channel.env.example ~/.claude-lab/myagent/secrets/channel.env
chmod 600 ~/.claude-lab/myagent/secrets/channel.env
nano ~/.claude-lab/myagent/secrets/channel.env
```
Заполни значения (имена переменных уже в файле, не трогай их):
```
TELEGRAM_BOT_TOKEN=токен_от_BotFather
TELEGRAM_ALLOWED_USER_IDS=твой_id
TELEGRAM_ALLOWED_CHAT_IDS=твой_id
GROQ_API_KEY=ключ_groq            # для голоса; см. Часть D
```
Сохрани: Ctrl+O, Enter, Ctrl+X.

`TELEGRAM_ALLOWED_USER_IDS` — единственный барьер доступа: бот слушает только эти
id. Больше туда никого не добавляй.

## Шаг 11 — Характер агента (CLAUDE.md)

Зачем: «душа» — имя, стиль, правила. То, что делает агента личностью.

**[СЕРВЕР]**:
```
nano ~/.claude-lab/myagent/.claude/CLAUDE.md
```
Минимум впиши: имя, стиль (по-русски, коротко, суть вперёд), правила
безопасности, и **ГЛАВНОЕ правило канала**:

> «Я общаюсь через Telegram, а не через терминал. Каждый ответ, вопрос,
> подтверждение и итог я отправляю пользователю инструментом reply. Текст в
> терминале пользователь не видит.»

Хочешь богаче — попроси самого Claude написать CLAUDE.md под твои задачи (см.
приём с интервью в Части E).

## Шаг 12 — Установить hooks (обязательно)

Зачем: без хуков нет пузырька прогресса, зеркала терминала и авто-дошылки
ответа.

**[СЕРВЕР]** (из папки `plugin`):
```
bash scripts/install-hooks.sh --settings ~/.claude/settings.json \
  --chat-id ТВОЙ_ID --webhook-url http://127.0.0.1:8089/hooks/agent \
  --agent-id dashi-channel
```

## Шаг 13 — Первый запуск (проверка, что мост живой)

> ⚠️ ГРАБЛЯ: запускать ТОЛЬКО из папки `plugin` — иначе «no MCP server».

**[СЕРВЕР]** (ты в `~/.claude-lab/myagent/.claude/dashi-plugin-claude-code/plugin`):
```
set -a
. ~/.claude-lab/myagent/secrets/channel.env
set +a
claude --dangerously-load-development-channels server:dashi-channel
```
При первом запуске Claude задаст 2 вопроса (внешние импорты + dev-каналы) —
ответь `1` на оба, это разово.

Теперь напиши боту в Telegram — он должен ответить. Работает — гаси пробный
запуск (Ctrl+C) и переходи к автономии и автозапуску.

## Шаг 14 — Диагностика «доктором» (если что-то не так)

Read-only проверка всего моста, ничего не ломает. Гоняй первым при любой
проблеме.

**[СЕРВЕР]** (из папки `plugin`):
```
bun ../skills/doctor-dashi-plugin/scripts/doctor.ts --plugin-dir "$PWD"
```
Код выхода 0 = всё чисто.

---

# ЧАСТЬ C. Полная автономия (ноль кнопок)

Зачем: чтобы агент сам делал действия и НЕ дёргал тебя подтверждениями на каждый
чих.

> ⚠️ ГЛАВНАЯ ГРАБЛЯ ДНЯ: режим «без подтверждений»
> (`--dangerously-skip-permissions`) под **root НЕ работает** — Claude выходит с
> ошибкой «cannot be used with root», сессия падает, systemd молча зациклится.
> Именно поэтому в Шаге 4 мы создали не-root юзера `agent`. Под ним автономия
> включается штатно. Если ты всё делал под `agent` — просто добавь флаг в запуск
> (уже вшит в systemd-юнит ниже, Часть F). Ничего отдельно менять не нужно.

Как проверить, что автономия реально работает: напиши боту «создай файл
/tmp/proba.txt с текстом привет и прочитай его». Если он сделал и отчитался БЕЗ
единого запроса подтверждения — автономия в строю.

---

# ЧАСТЬ D. Голос (расшифровка голосовых)

Зачем: без ключа Groq текст работает, а голос — нет.

1. **[БРАУЗЕР]** console.groq.com → залогинься → «Create API Key» → скопируй
   (начинается на `gsk_`). Бесплатно, лимитов на голос с запасом хватает.
2. Впиши ключ в `channel.env` (строка `GROQ_API_KEY=...`, Шаг 10) и перезапусти
   сервис (`systemctl restart ...` из Части F).

---

# ЧАСТЬ E. Автозапуск 24/7 (systemd)

Зачем: агент стартует сам при перезагрузке и поднимается после сбоя. Claude
Code — интерактивная программа, ей нужен «экран», поэтому держим её в tmux.

Проще всего НЕ писать юнит руками, а прогнать установщик (шаг 2 быстрого пути,
`--user agent`) — он ставит уже отлаженную схему. Живой день отладки 20.08.2026
показал, что наивный юнит ломается сразу тремя способами:

> ⚠️ ГРАБЛЯ: `Type=forking` + tmux — мина. systemd теряет главный процесс
> демонизировавшегося tmux и через секунду сам гасит только что поднятого
> агента. Рабочая схема: `Type=simple` + скрипт-держатель, который поднимает
> сессию и живёт, пока жива она.
> ⚠️ ГРАБЛЯ: слепые `send-keys Enter` по таймеру жмут не те кнопки: набор
> стартовых вопросов от запуска к запуску РАЗНЫЙ (принятые запоминаются), и
> «вниз+Enter» попадал в диалог dev-каналов, выбирая «Exit» — сервис сам убивал
> Claude. Жать надо глядя на экран (`tmux capture-pane` → нужная клавиша).
> ⚠️ ГРАБЛЯ: запуск голого `claude` в юните умирает на окружении — запускать
> через `/bin/bash -lc '...'`, чтобы подтянулся PATH юзера (bun и прочее).

Если всё же руками — установщик кладёт два скрипта, повтори их логику:
`/usr/local/bin/dashi-run <сессия> <папка-plugin>` (kill-session → new-session
с `bash -lc 'cd ... && exec claude ...'` → проверка, что tmux-сессия реально
поднялась → фоновый прожиматель диалогов → цикл раз в 15 сек, пока жива
сессия) и `/usr/local/bin/dashi-press-dialogs` (смотрит экран до 2 минут —
слабый VPS грузит Claude дольше 45 сек: «Bypass Permissions» → Down+Enter,
остальные диалоги → Enter).

В цикле dashi-run сидит ещё и сторож моста: он щупает вебхук-порт
(127.0.0.1:8089). Порт был жив и пропал на 2 проверки подряд → dashi-run
выходит ненулевым, systemd перезапускает всё целиком. Это закрывает классику
«tmux жив, мост сдох»: systemd доволен, бот молчит, чинить только терминалом.
До первого подъёма порта сторож не проверяет — на свежей установке Claude
может сколько угодно стоять на экране логина.

И мелочь, которая спасает диск: установщик кладёт drop-in
`/etc/systemd/journald.conf.d/dashi.conf` с `SystemMaxUse=500M` — без потолка
журнал systemd за месяцы съедает гигабайты на маленьком VPS.
Юнит: `Type=simple`, `ExecStart=/usr/local/bin/dashi-run ...`,
`ExecStop=tmux kill-session`, `Restart=always`, `RestartSec=15s`, плюс
`EnvironmentFile` с токенами и `Environment=HOME=...`. Точный текст — в
`scripts/install-agent.sh` репозитория, секция «8. systemd».

Проверь автозапуск после перезагрузки: `systemctl is-enabled dashi-<имя>` → `enabled`.

Приём для богатого характера: скорми боту файл-интервью — попроси задать по
порядку вопросы (чем занимаешься, как обращаться, стиль, часовой пояс, запреты) и
записать ответы в CLAUDE.md. Тогда характер соберётся сам, без ручной писанины.

---

# ЧАСТЬ F. Память агента (помнит между сессиями)

Зачем: чтобы агент помнил тебя, проект и твои правки, а не начинал с нуля.

Самый простой рабочий вариант — файловая память. Скорми боту **одним
сообщением** ТЗ:

**[БОТУ]**:
```
Организуй себе долгую память. Создай в моём workspace (рядом с CLAUDE.md):
- папку memory/ и в ней файл MEMORY.md — индекс, одна строка на факт
  «- [Заголовок](файл.md) — короткий хук»;
- каждый факт — свой файл memory/имя.md с шапкой (name, description,
  type: user/project/feedback/reference) и телом-фактом.
В CLAUDE.md впиши правила: при старте читаю MEMORY.md; узнал факт или получил
правку — сохраняю новый файл + строку в индекс; память против реальности —
реальность выше.
Заполни сразу файл обо мне: кто я, чем занимаюсь, как обращаться, часовой пояс.
```
Дальше просто говоришь «запомни, что…» — копит сам.

Хочешь многослойку (как у продвинутых агентов): добавь папку `core/` с файлами
`USER.md`, `rules.md` (правила и правки), `decisions.md` (решения ~14 дней),
`handoff.md` (последние 5 итогов), `LEARNINGS.md` (уроки), а в CLAUDE.md через
`@include` подключи ТОЛЬКО лёгкие `USER.md` и `rules.md` — остальное агент читает
по требованию, чтоб не жечь контекст. Тоже отдаётся боту одним ТЗ.

---

# ЧАСТЬ G. Бот-ремонтник (страховка на случай, когда основной завис)

Зачем: если основной агент завис и молчит — через него же ты его не починишь.
Нужен ОТДЕЛЬНЫЙ бот на отдельном процессе, который умеет его перезапускать.

Ключевой принцип: ремонтник — **тупой командный скрипт, НЕ на Claude**. В этом
его надёжность: голый скрипт на пяти командах не виснет, а значит всегда сможет
поднять основного. Будь он на том же движке — завис бы вместе с ним.

**Шаг 1 — второй бот.** @BotFather → `/newbot` → новый токен. Найди его, Start,
«привет».

**Шаг 2 — код напишет сам основной агент** (пока он живой). Скорми ему:

**[БОТУ]**:
```
Напиши мне бота-ремонтника — отдельный сторож за сервисом dashi.
1. Python-скрипт на long-polling (только requests). Слушает ВТОРОГО бота.
   Токен второго бота и мой user_id читает из /home/agent/repairbot.env
   (REPAIR_BOT_TOKEN и REPAIR_OWNER_ID).
2. Реагирует ТОЛЬКО на мой user_id, остальных игнорит.
3. Команды: /ping (ответ «жив»), /status (systemctl is-active dashi + аптайм),
   /restart, /stop, /start, /logs (последние 40 строк journalctl -u dashi).
   Результат шлёт мне в чат, длинное режет до 4000 символов.
4. Юнит repairbot.service от root, автозапуск, Restart=on-failure.
Ты под юзером agent и в /root, /etc писать не можешь. Поэтому запиши файлы в мою
папку: скрипт в /home/agent/repairbot.py, юнит в /home/agent/repairbot.service.
Потом дай мне точные команды, которыми я от root перенесу их на места и включу.
```

> ⚠️ ГРАБЛЯ: не проси бота вываливать код в ЧАТ и копировать вручную — длинный
> код через Telegram копируется криво. Пусть пишет файлами на диск (у юзера
> `agent` в своей папке права есть), а ты переносишь их от root.

**Шаг 3 — установка от root** (основной агент выдаст токен-плейсхолдеры, ты
подставишь). **[СЕРВЕР]** под root:
```
cp /home/agent/repairbot.py /root/repairbot.py
cp /home/agent/repairbot.service /etc/systemd/system/repairbot.service
nano /root/repairbot.env
      # REPAIR_BOT_TOKEN=токен_второго_бота
      # REPAIR_OWNER_ID=твой_id
chmod 600 /root/repairbot.env
systemctl daemon-reload
systemctl enable --now repairbot
systemctl status repairbot     # ждём active (running)
```

**Шаг 4 — проверка.** Напиши ВТОРОМУ боту `/ping` (ответит «жив») и `/status`
(покажет состояние основного). Отвечает — страховка на месте.

**Как чинить, если /restart не помогает:**
1. `/logs` — глянь причину (частое: перегруз сервера/кончилась память, протух
   логин Claude, битый конфиг).
2. По SSH: `systemctl status dashi`, `journalctl -u dashi -n 50`. Если висит в
   петле — реальная ошибка часто в самом окне Claude: `tmux attach -t dashi`.
3. Глухо висит весь сервер — `reboot` целиком (оба сервиса в автозапуске,
   встанут сами).

---

# Карта файлов

```
~/.claude/                                               — вход в подписку (СЕКРЕТ)
~/.claude/settings.json                                  — сюда встают hooks
~/.claude-lab/myagent/.claude/CLAUDE.md                  — характер агента
~/.claude-lab/myagent/.claude/dashi-plugin-claude-code   — плагин (ВНУТРИ workspace!)
~/.claude-lab/myagent/secrets/channel.env                — токен, id, ключ голоса (chmod 600)
~/.claude-lab/myagent/.claude/memory/                    — память агента
/etc/systemd/system/dashi.service                        — автозапуск основного
/usr/local/bin/dashi-run + dashi-press-dialogs           — держатель сессии + прожиматель диалогов
/usr/local/bin/dashi-ctl-<имя> + /etc/sudoers.d/dashi-<имя> — самообслуживание агента (root-owned)
/root/repairbot.py + /etc/systemd/system/repairbot.service — бот-ремонтник
```

---

# Железное правило безопасности

Любую починку/переустановку МОСТА делай из **терминала сервера, НЕ из Telegram**.
Ты меняешь тот самый канал, которым шлёшь команды — сломается на середине,
Telegram замолчит, и чинить будет нечем. Порядок: терминал на сервере → правка →
доктор → тестовое сообщение → только потом ушёл. (Именно для аварий из терминала
и существует бот-ремонтник — он единственное исключение, потому что он отдельный
процесс.)

---

# Быстрый траблшутинг (симптом → причина → фикс)

| Симптом | Причина | Фикс |
|---|---|---|
| Бот молчит после запуска | Запустил не из папки `plugin` | `cd .../plugin`, запускай оттуда |
| «no MCP server configured» | То же — неверная папка | Запуск из `plugin` |
| Claude падает сразу, tmux пуст | Режим без подтверждений под root | Перенести на не-root юзера (Часть C) |
| Спрашивает подтверждения на всё | Нет `--dangerously-skip-permissions` / под root | Не-root юзер + флаг в юните |
| Вход в Claude не проходит | Пытаешься через claude.ai/приложение | OAuth прямо на сервере (Шаг 5) |
| Голос не расшифровывается | Нет `GROQ_API_KEY` | Впиши ключ, перезапусти сервис |
| Код из чата вставляется криво | Копипаст длинного кода в терминал | Пусть агент пишет файлы на диск |
| Сессия зависла на старте | Не «нажаты» 2 первых вопроса | `ExecStartPost` с двумя Enter; вручную: `tmux send-keys -t channel-<имя> Enter` (без attach) |
| Claude живой, бот молчит, на экране «bun: not found» | Сессию подняли руками из `su agent` БЕЗ дефиса или под root | `systemctl restart dashi-<имя>`; руками — только после `su - agent` |
| Молчит, сторож написал «вход в Claude протух» | Месячный перелогин | Отправь боту `/relogin`; мост тоже лежит — раздел «Раз в ~30 дней» выше |
| Агент: «файл принадлежит root, записать не могу» | Что-то запускали/правили под root в его папках | Попроси агента: `sudo dashi-ctl-<имя> fix-owner`; папки вне его дома — под root `chown -R agent:agent <папка>` |
| Агент просит выполнять команды под root | Нет dashi-ctl / секции «Самообслуживание» в CLAUDE.md | Раздел «Чтобы агент всё делал сам» выше |
| Бот тупит, но команды слышит | Мост подзавис | `/restart` боту, подожди минуту |
| Основной завис, не отвечает | Разное | Ремонтнику `/restart`, не помог — `reboot` |

Репозиторий плагина: github.com/qwwiwi/dashi-plugin-claude-code
(docs/02-where-to-place-plugin.md, docs/03-installation-linux.md,
docs/05-troubleshooting.md, skills/doctor-dashi-plugin).
